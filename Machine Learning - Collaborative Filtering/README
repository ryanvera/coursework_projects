The goal of this project was to implement a collaborative filtering algorithm, as known as a recommendation system.

The algorithm implemented is a memory-based algorithm. The data used to train the model was a subset of the Netflix data released for
research pruposes.
